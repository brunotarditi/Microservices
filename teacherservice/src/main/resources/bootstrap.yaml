spring:
  application:
    name: teacherservice
  config:
    import: optional:configserver:http://localhost:8081

  data:
    mongodb:
      host: localhost
      port: 27017
      database: microservices