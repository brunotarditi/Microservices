spring:
  application:
    name: universityservice
  cloud:
    config:
      enabled: true
      uri: http://configservice:8888

  data:
    mongodb:
      host: localhost
      port: 27017
      database: microservices